# Launch note

Suggested first tweet:

> it only takes one sloperator to trust another sloperator’s AI-generated PR and suddenly a random dependency is reading env vars, curling a pastebin, and calling it “telemetry.”
>
> boom: supply chain attack.

Suggested repo tagline:

> Deterministic PR checks for dependency, lockfile, and workflow changes that smell like supply-chain trouble.
